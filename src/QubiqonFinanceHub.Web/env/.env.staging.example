# Copy to `.env.staging` (ignored by git) for stage builds (`npm run build:stage`).
# Stage deployed: https://api-stage-finance.qubiqon.io/api
VITE_API_BASE_URL=https://api-stage-finance.qubiqon.io/api
